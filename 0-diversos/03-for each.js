/*
    foreach: E um tipo de laço for especialisado para percorrer arrays, ela retorna, o item, a sua posição e o array completo, sendo os dois ultimos opcionais, e estes são passados como parametros para a função que criamos dentro do for.
    
    SINTAXE:
    
    nome_do_array.forEach(funtion(){
    })

    nome_do_array.forEach(parametros => {
            INSTRUÇÔES
        })
*/