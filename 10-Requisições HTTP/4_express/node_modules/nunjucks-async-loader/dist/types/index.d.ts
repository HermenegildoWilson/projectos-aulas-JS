import { Loader, type LoaderSource, type LoaderOptions, type Callback } from 'nunjucks';
import { type FSWatcher } from 'chokidar';
export declare class FileSystemAsyncLoader extends Loader {
    async: boolean;
    pathsToNames: Record<string, string>;
    searchPaths: string[];
    noCache: boolean;
    fsWatcher?: FSWatcher;
    constructor(searchPaths: string[] | string, opts?: LoaderOptions);
    watchDirs(searchPaths: string[]): Promise<void>;
    getSourceAsync(name: string): Promise<LoaderSource>;
    destroy(): Promise<void>;
    getSource(name: string, callback: Callback<Error, LoaderSource>): void;
}
export default FileSystemAsyncLoader;
